pipeline {
    agent { label 'slave1' }

    stages {
        stage('Test') {
            steps {
                sh 'echo "Hello from Jenkins Agent!"'
                sh 'hostname'
                sh 'whoami'
                sh 'pwd'
            }
        }
    }
}
